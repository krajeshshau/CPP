#include <iostream>

using namespace std;

//plain enum
enum color{red,green,yellow,black};
enum people{good,bad};

int main()
{
    if(red == good) cout<<"Magic in enum"<<endl;
    else cout<<"All is well in enum"<<endl;
    return 0;
}
/* OP
Magic in enum                                                                   
*/
